## 哈希

### 1. 两数之和

---

#### 思路

`两数之和`（Two Sum）是 LeetCode 第 1 题，可以用 **哈希表** 来实现高效解法。思路是：

- 遍历数组，每次计算目标值 `target - nums[i]` 是否已经在哈希表里。
- 如果在，说明找到了两个数；
- 如果不在，把当前数存到哈希表里（键为数值，值为索引）。

时间复杂度是 **O(n)**，空间复杂度是 **O(n)**。

---

下面是 Java 代码实现：

```java
import java.util.HashMap;
import java.util.Map;

class Solution {
    public int[] twoSum(int[] nums, int target) {
        // 哈希表存储数值和对应的索引
        Map<Integer, Integer> map = new HashMap<>();
        
        for (int i = 0; i < nums.length; i++) {
            int need = target - nums[i];
            // 如果哈希表里已经有了 need，就直接返回
            if (map.containsKey(need)) {
                return new int[] { map.get(need), i };
            }
            // 否则把当前数放入哈希表
            map.put(nums[i], i);
        }
        
        /*
        // 题目保证一定有解，这里不会执行
        throw new IllegalArgumentException("No solution found");
        */
    }
}
```

🔑 核心点：

- `map.containsKey(need)` 用来判断之前是否出现过满足条件的数。
- `map.put(nums[i], i)` 保证了后续能找到索引。

---

##### 时空复杂度

###### ⏱ 时间复杂度分析

算法流程是：

1. 遍历整个数组 `nums`，长度为 `n`。
2. 对于每个元素 `nums[i]`：
   - 计算 `need = target - nums[i]`（**O(1)** 操作）。
   - 在哈希表里查找 `need`（HashMap 的 `containsKey` 平均复杂度 **O(1)**）。
   - 如果没找到，就 `put(nums[i], i)` 插入哈希表（平均 **O(1)**）。

所以每个元素的处理都是 **常数时间 O(1)**，
 总共有 `n` 个元素，整体就是：

T(n)=O(n)×O(1)=O(n)

> **结论**：时间复杂度是 **O(n)**。

###### 💾 空间复杂度分析

额外使用了一个 **哈希表 map**：

- 在最坏情况下（比如数组里没有任何一对数能提前满足条件，直到最后才找到），我们会把 **n 个元素都存到哈希表里**。
- 哈希表存储的是 **数值 -> 索引**，每个元素只存一条映射，所以需要的额外空间是 **O(n)**。

因此空间复杂度为：

S(n)=O(n)

###### ⚖️ 总结

- **时间复杂度**：O(n)
- **空间复杂度**：O(n)

相比之下，如果用 **暴力解法**（双重循环），时间复杂度是 **O(n²)**，空间复杂度是 **O(1)**。
 所以哈希表解法在大数据规模下效率明显更高。

------







### 49.字母异位词分组

---

#### 思路

这个问题是 **LeetCode 49. 字母异位词分组 (Group Anagrams)**，用哈希表可以高效解决。

- **异位词**的特点：排序后字符串是一样的。
- 所以我们可以用 **排序后的字符串作为哈希表的 key**，把相同 key 的原始字符串放在一起。

举例：
 输入 `["eat", "tea", "tan", "ate", "nat", "bat"]`
 排序后：

- `"eat" → "aet"`
- `"tea" → "aet"`
- `"tan" → "ant"`
- `"ate" → "aet"`
- `"nat" → "ant"`
- `"bat" → "abt"`

最后哈希表存储：

- `"aet" -> ["eat","tea","ate"]`
- `"ant" -> ["tan","nat"]`
- `"abt" -> ["bat"]`

取出所有 value 就是结果。

---

```java
import java.util.*;

class Solution {
    public List<List<String>> groupAnagrams(String[] strs) {
        // 哈希表：key 是排序后的字符串，value 是一组异位词
        Map<String, List<String>> map = new HashMap<>();
        
        for (String s : strs) {
            // 1. 把字符串转成 char[]，排序，再转回字符串
            char[] arr = s.toCharArray();
            Arrays.sort(arr);
            String key = new String(arr);
            
            // 2. 放入哈希表
            map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);
        }
        
        // 3. 返回哈希表里的所有 value
        return new ArrayList<>(map.values());
    }
}
```

---

##### computeIfAbsent 

没问题 👍，这行代码其实是 **Java 8 提供的一个简化写法**，我来帮你拆开分析：

```java
map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);
```

###### 1. `map.computeIfAbsent(key, mappingFunction)`

- 作用：如果 `map` 中已经有 `key`，直接返回对应的 `value`；
- 如果没有 `key`，就调用 `mappingFunction` 来生成一个新的 `value`，放进 `map`，然后返回这个新的 `value`。

换句话说，它的逻辑相当于：

```java
if (!map.containsKey(key)) {
    map.put(key, new ArrayList<>()); // 不存在就创建一个新的 list
}
List<String> list = map.get(key); // 一定能拿到一个 list
```

###### 2. `k -> new ArrayList<>()`

这是一个 **Lambda 表达式**，等价于一个函数：

- 参数是 `k`（其实就是 `key`，这里没用上）。
- 返回值是 `new ArrayList<>()`。

意思是：当 `key` 不存在时，就生成一个新的 `ArrayList`。

###### 3. `.add(s)`

前面 `computeIfAbsent` 返回的是一个 `List<String>`（一定存在）。
 所以我们直接在这个 `List` 上调用 `.add(s)`，把当前的字符串 `s` 加进去。

###### 4. 合并理解

所以这一行代码的完整逻辑是：

1. 判断哈希表里是否存在 `key`；
2. 如果不存在，创建一个新的 `ArrayList` 并放进去；
3. 返回对应的 `List<String>`；
4. 把当前字符串 `s` 加到这个 list 里。

等价于下面的写法（传统写法）：

```java
if (!map.containsKey(key)) {
    map.put(key, new ArrayList<>());
}
map.get(key).add(s);
```

✅ 所以 `computeIfAbsent` 就是一个 **更简洁优雅的写法**。

---

##### 时空复杂度

###### 时间复杂度分析

设：

- `n` = 字符串个数
- `k` = 字符串的最大长度

1. 遍历 `n` 个字符串。
2. 每次排序一个字符串，复杂度是 `O(k log k)`。
3. 插入哈希表是 `O(1)` 平均复杂度。

总时间复杂度：

O(n⋅klog⁡k)

###### 空间复杂度分析

- 哈希表存储所有字符串的分组，最多存 `n` 个字符串。
- 排序需要 `O(k)` 的额外空间（`char[]`）。

总空间复杂度：

O(n⋅k)

---







#### 思路2

👉 除了 **排序作为 key**，还可以用 **字符计数作为 key**（比如 26 个字母的频率数组转成字符串），这样每个字符串处理是 **O(k)**，整体时间复杂度能降到 **O(n · k)**。这个方法相比排序的方式更高效。

##### 🔑 核心思路

- **异位词**：它们包含的 **字符种类和出现次数完全相同**。
- 所以我们不用排序，而是统计每个字符串中 **26 个字母出现的频率**。
- 再把这个频率数组转成一个字符串，作为哈希表的 key。

##### 示例

输入：`["eat","tea","tan","ate","nat","bat"]`

- `"eat"`：`[1,0,0,...,1,...,1,0,...] → "1#0#0#...#1#..."`
- `"tea"`：一样的频率数组 → key 相同
- `"tan"`：另一个 key
- `"bat"`：又是一个 key

这样同一个 key 的字符串就是一组异位词。

---

##### 💻 Java 代码实现

```java
import java.util.*;

class Solution {
    public List<List<String>> groupAnagrams(String[] strs) {
        Map<String, List<String>> map = new HashMap<>();

        for (String s : strs) {
            // 1. 统计字符频率
            int[] count = new int[26];
            for (char c : s.toCharArray()) {
                count[c - 'a']++;
            }

            // 2. 把频率数组转成 key（用 # 分隔，避免混淆）
            StringBuilder sb = new StringBuilder();
            for (int num : count) {
                sb.append(num).append('#');
            }
            String key = sb.toString();

            // 3. 放入哈希表
            map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);
        }

        return new ArrayList<>(map.values());
    }
}
```

---

##### 时空复杂度

###### ⏱ 时间复杂度

- 遍历 `n` 个字符串。
- 每个字符串统计字符频率：`O(k)`（`k` 是字符串长度）。
- 拼接 key 的时候固定 26 次循环：`O(26) = O(1)`。

总时间复杂度：

O(n⋅k)

⚡ 比排序解法的 `O(n*klogk)` 更快。

###### 💾 空间复杂度

- 哈希表需要存储最多 `n` 个 key-value 对。
- 每个 key 是 26 长度的计数串，空间近似 `O(26n)` → `O(n)`。
- 总的空间复杂度：

O(n⋅k)

------

📌 **对比总结：**

- **排序解法**：代码更直观，复杂度 `O(n·k log k)`。
- **字符计数解法**：代码稍微复杂，但更高效，复杂度 `O(n·k)`。

------









### 128.最长连续序列

----

#### 🔑 思路

用 **哈希表 (HashSet)** 能在 **O(n)** 时间解决，比排序解法更高效。

1. 把所有数字放到 `HashSet`，这样能 **O(1)** 时间判断某个数是否存在。
2. 遍历每个数 `num`，检查它是不是一个连续序列的 **起点**：
   - 条件：`num - 1` 不在集合中（说明 `num` 是序列开头）。
3. 如果是起点，就一直往后找 `num+1, num+2, ...`，直到找不到为止，统计这个序列的长度。
4. 更新最长长度。

⚡ 这样每个数只会被访问一次，整体复杂度是 **O(n)**。

---

##### 💻 Java 代码

```java
import java.util.HashSet;
import java.util.Set;

class Solution {
    public int longestConsecutive(int[] nums) {
        Set<Integer> set = new HashSet<>();
        for (int num : nums) {
            set.add(num);
        }

        int longest = 0;

        for (int num : set) {
            // 只在序列开头才开始统计，避免重复计算
            if (!set.contains(num - 1)) {
                int currentNum = num;
                int length = 1;

                while (set.contains(currentNum + 1)) {
                    currentNum++;
                    length++;
                }

                longest = Math.max(longest, length);
            }
        }

        return longest;
    }
}
```

------

##### 时空复杂度

###### ⏱ 时间复杂度

- 建立 `HashSet`：O(n)
- 遍历 `set` 中的每个数：O(n)
- 每个元素最多被访问两次（一次起点判断，一次在 while 里扩展）。

总复杂度=O(n)

###### 💾 空间复杂度

- `HashSet` 存储所有数字：O(n)

------

✅ 对比：

- **排序解法**：O(n log n)，空间 O(1)。
- **哈希解法**：O(n)，空间 O(n)。

哈希法更快，更适合大输入。

---







## 双指针

### 283.移动零

---

#### 思路

将数组中的所有 **0** 移动到数组末尾，同时保持非零元素的相对顺序。**必须在原数组上操作，不能拷贝额外的数组。**

- 使用两个指针：
  - **fast**：遍历数组
  - **slow**：指向下一个非零元素应该放置的位置
- 遍历时：
  - 如果 `nums[fast] != 0`，就把它放到 `nums[slow]`，然后 `slow++`。
  - 遍历结束后，`slow` 之后的位置全填 `0`。

---

##### Java代码实现

```java
class Solution {
    public void moveZeroes(int[] nums) {
        int n = nums.length;
        int slow = 0;

        // 1. 遍历数组，把非零元素依次放到前面
        for (int fast = 0; fast < n; fast++) {
            if (nums[fast] != 0) {
                nums[slow] = nums[fast];
                slow++;
            }
        }

        // 2. slow 之后的元素全设为 0
        while (slow < n) {
            nums[slow] = 0;
            slow++;
        }
    }
}
```

---

##### 时空复杂度

- **时间复杂度**: O(n)，只遍历一次数组
- **空间复杂度**: O(1)，原地修改

---





### 11.盛最多水的容器

------

#### 思路（双指针解法）

- 定义两个指针：

  - **left** 在最左边，
  - **right** 在最右边。

- 每次计算当前容器的面积：

  ```java
  area = min(height[left], height[right]) * (right - left);
  ```

- 更新最大值。

- 然后移动 **较短的那一边**：

  - 如果 `height[left] < height[right]`，左指针右移；
  - 否则右指针左移。

- 为什么移动短的一边？
   因为面积由 **宽度 \* 高度** 决定，宽度每次都会减小，想要增大面积只能提高高度，而高度取决于较短的边。

---

##### Java 代码实现

```java
class Solution {
    public int maxArea(int[] height) {
        int left = 0;
        int right = height.length - 1;
        int max = 0;

        while (left < right) {
            int h = Math.min(height[left], height[right]);
            int area = h * (right - left);
            max = Math.max(max, area);

            // 移动短板
            if (height[left] < height[right]) {
                left++;
            } else {
                right--;
            }
        }
        return max;
    }
}
```

------

##### 时空复杂度

- **时间复杂度**: O(n)，左右指针最多各移动一次
- **空间复杂度**: O(1)，只用常数额外变量

------







### 15.三数之和

---

#### 思路（排序 + 双指针）

1. **排序**：先对数组排序，方便去重和双指针移动。
2. **枚举第一个数**：用下标 `i` 固定第一个数 `nums[i]`。
   - 如果 `nums[i] > 0`，直接 break，因为三数和不可能为 0。
   - 如果和前一个相同，跳过（去重）。
3. **双指针找剩余两个数**：
   - 定义 `left = i + 1`，`right = n - 1`。
   - 计算三数和 `sum = nums[i] + nums[left] + nums[right]`：
     - 如果 `sum == 0`，记录答案，并移动指针，同时去重。
     - 如果 `sum < 0`，说明需要更大一点 → `left++`。
     - 如果 `sum > 0`，说明需要更小一点 → `right--`。

---

##### Java 代码实现

```java
import java.util.*;

class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        List<List<Integer>> res = new ArrayList<>();
        Arrays.sort(nums); // 1. 排序
        int n = nums.length;

        for (int i = 0; i < n - 2; i++) {
            // 去重: 如果当前数和前一个一样，跳过
            if (i > 0 && nums[i] == nums[i - 1]) continue;

            // 如果当前数已经大于 0，直接结束
            if (nums[i] > 0) break;

            int left = i + 1, right = n - 1;
            while (left < right) {
                int sum = nums[i] + nums[left] + nums[right];

                if (sum == 0) {
                    res.add(Arrays.asList(nums[i], nums[left], nums[right]));

                    // 跳过重复元素
                    while (left < right && nums[left] == nums[left + 1]) left++;
                    while (left < right && nums[right] == nums[right - 1]) right--;

                    left++;
                    right--;
                } else if (sum < 0) {
                    left++; // 需要更大
                } else {
                    right--; // 需要更小
                }
            }
        }

        return res;
    }
}
```

------

##### 时空复杂度

- **时间复杂度**: O(n²)，外层遍历 `i`，内层双指针是 O(n)。
- **空间复杂度**: O(1)，结果集不算额外空间。

------











### 42.接雨水

---

#### 思路（双指针 + 动态维护左右最高）

1. 用两个指针：
   - `left` 从左往右
   - `right` 从右往左
2. 同时维护：
   - `leftMax`：从左边到当前位置的最高柱子高度
   - `rightMax`：从右边到当前位置的最高柱子高度
3. 每次比较 `height[left]` 和 `height[right]`：
   - 如果 `height[left] < height[right]`：
     - 说明左边的最高值决定了能接多少水
     - 如果 `height[left] < leftMax`，可以接水 `leftMax - height[left]`
     - 否则更新 `leftMax`
     - 然后 `left++`
   - 否则对称处理右边：
     - 如果 `height[right] < rightMax`，可以接水 `rightMax - height[right]`
     - 否则更新 `rightMax`
     - 然后 `right--`

这样我们就可以在一次扫描里完成计算。

---

##### Java 代码实现

```java
class Solution {
    public int trap(int[] height) {
        int left = 0, right = height.length - 1;
        int leftMax = 0, rightMax = 0;
        int res = 0;

        while (left < right) {
            if (height[left] < height[right]) {
                if (height[left] >= leftMax) {
                    leftMax = height[left];
                } else {
                    res += leftMax - height[left];
                }
                left++;
            } else {
                if (height[right] >= rightMax) {
                    rightMax = height[right];
                } else {
                    res += rightMax - height[right];
                }
                right--;
            }
        }

        return res;
    }
}
```

------

##### 时空复杂度

- **时间复杂度**: O(n)，只遍历一次
- **空间复杂度**: O(1)，常数额外空间

------







#### 思路2

要不要我再帮你写一个 **用单调栈** 的解法？有时候面试官会追问你能不能用不同思路解。









## 滑动窗口

### 3.无重复字符的最长子串

---

#### 思路

1. 使用 **双指针（滑动窗口）**：`left` 和 `right` 表示当前窗口范围。
2. 使用 **HashSet** 或 **HashMap** 来记录窗口中的字符。
3. 每次右指针 `right` 往右移动，遇到重复字符时，移动左指针 `left`，直到窗口内没有重复字符。
4. 在移动过程中更新最大长度。

------

##### Java 实现（推荐写法：HashSet）

```java
import java.util.HashSet;
import java.util.Set;

public class Solution {
    public int lengthOfLongestSubstring(String s) {
        Set<Character> window = new HashSet<>();
        int left = 0, right = 0, maxLen = 0;
        
        while (right < s.length()) {
            char c = s.charAt(right);
            // 如果有重复字符，则缩小窗口
            while (window.contains(c)) {
                window.remove(s.charAt(left));
                left++;
            }
            // 加入当前字符
            window.add(c);
            // 更新最大长度
            maxLen = Math.max(maxLen, right - left + 1);
            right++;
        }
        
        return maxLen;
    }
}
```

------

##### Java 实现（优化写法：HashMap 记录字符索引）

> 用 `HashMap<Character, Integer>` 记录字符上一次出现的位置，可以直接跳过，减少左指针的移动次数。

```java
import java.util.HashMap;
import java.util.Map;

public class Solution {
    public int lengthOfLongestSubstring(String s) {
        Map<Character, Integer> map = new HashMap<>();
        int left = 0, maxLen = 0;
        
        for (int right = 0; right < s.length(); right++) {
            char c = s.charAt(right);
            // 如果字符出现过，更新 left
            if (map.containsKey(c)) {
                left = Math.max(left, map.get(c) + 1);
            }
            // 记录当前字符位置
            map.put(c, right);
            maxLen = Math.max(maxLen, right - left + 1);
        }
        
        return maxLen;
    }
}
```

------

##### 时空复杂度

- **时间复杂度**：O(n)，每个字符最多被访问两次（左右指针各一次）。
- **空间复杂度**：O(min(n, charset_size))，常见情况是 O(字符集大小)，ASCII 是 128。

------











### 438.找到字符串中所有字母异位词

---

#### 思路

1. 我们需要找到字符串 `s` 中所有与 `p` 的 **异位词**（字母相同、顺序可以不同）。
2. 核心方法：
   - 用 **两个计数数组** `countP` 和 `countS`，长度 26，分别记录 `p` 和当前窗口中每个字母出现次数。
   - 使用 **固定长度滑动窗口**（大小等于 `p.length()`）。
   - 每次移动窗口时，更新窗口计数，并判断窗口和 `countP` 是否相等。
3. 如果相等，就说明窗口内的子串是一个异位词，记录起始索引。

------

##### Java 实现

```java
import java.util.ArrayList;
import java.util.List;

public class Solution {
    public List<Integer> findAnagrams(String s, String p) {
        List<Integer> result = new ArrayList<>();
        if (s.length() < p.length()) {
            return result;
        }

        // 统计 p 的字母频率
        int[] countP = new int[26];
        int[] countS = new int[26];
        for (char c : p.toCharArray()) {
            countP[c - 'a']++;
        }

        int left = 0, right = 0;
        while (right < s.length()) {
            // 扩展右窗口
            countS[s.charAt(right) - 'a']++;
            // 如果窗口大小超过 p 的长度，缩小左边
            if (right - left + 1 > p.length()) {
                countS[s.charAt(left) - 'a']--;
                left++;
            }
            // 如果窗口大小和 p 一样，比较是否相同
            if (right - left + 1 == p.length()) {
                if (isEqual(countP, countS)) {
                    result.add(left);
                }
            }
            right++;
        }

        return result;
    }

    // 判断两个频率数组是否相等
    private boolean isEqual(int[] a, int[] b) {
        for (int i = 0; i < 26; i++) {
            if (a[i] != b[i]) {
                return false;
            }
        }
        return true;
    }
}
```

------

##### 时空复杂度

- **时间复杂度**：O(n * 26) ≈ O(n)，其中 n = s.length()，因为每次比较两个频率数组需要 26 次。
- **空间复杂度**：O(1)，固定 26 个字母。

------

⚡优化思路：

 可以维护一个 `match` 变量，记录当前窗口中有多少个字符频率和 `p` 对上了，就不用每次比较数组（把 `O(26)` 优化成 `O(1)`）。







## 子串

### 560.和为 K 的子数组

---

#### 思路

##### Z：公式变形。

------

##### ✅ 前缀和 + HashMap 解法（带复杂度分析）

```java
import java.util.HashMap;

public class Solution {
    public int subarraySum(int[] nums, int k) {
        // 存储 前缀和 -> 出现次数
        HashMap<Integer, Integer> map = new HashMap<>();
        map.put(0, 1); // 前缀和为0出现1次，处理边界情况

        int count = 0; // 结果：子数组个数
        int sum = 0;   // 当前前缀和

        for (int num : nums) {
            sum += num; // 更新前缀和

            // 如果 sum-k 在 map 中，说明存在子数组和为 k
            if (map.containsKey(sum - k)) {
                count += map.get(sum - k);
            }

            // 更新前缀和出现次数
            map.put(sum, map.getOrDefault(sum, 0) + 1);
        }

        return count;
    }
}
```

------

##### 📊 时空复杂度

1. **时间复杂度：O(n)**
   - 遍历数组一次，每个元素只进行 **O(1)** 的 HashMap 操作（`get`、`put`）。
   - 整体复杂度是 **O(n)**。
2. **空间复杂度：O(n)**
   - 最坏情况下，每个前缀和都不同，需要存储 `n` 个前缀和。
   - 所以空间复杂度是 **O(n)**。

------









### 239.滑动窗口最大值

---

#### 思路

这个题有一个经典高效解法，时间复杂度是 O(n)，用 **双端队列（Deque）** 来维护窗口内的候选最大值。

1. 使用一个 **双端队列 deque** 来存储数组下标。
2. 队列的维护原则：
   - 队首总是当前窗口的最大值下标。
   - 当新元素 `nums[i]` 来时，把队尾比它小的元素都弹出（因为它们不可能成为最大值）。
3. 当窗口形成时（i >= k-1）：
   - 队首的元素就是当前窗口的最大值。

------

##### ✅ Java 实现（带详细注释）

```java
import java.util.*;

public class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        if (nums == null || k <= 0) return new int[0];
        
        int n = nums.length;
        int[] res = new int[n - k + 1]; // 结果数组
        int ri = 0; // 结果数组索引
        
        Deque<Integer> deque = new LinkedList<>(); // 存下标
        
        for (int i = 0; i < n; i++) {
            // 1. 移除 deque 中已经不在窗口范围的下标
            while (!deque.isEmpty() && deque.peekFirst() < i - k + 1) {
                deque.pollFirst();
            }
            
            // 2. 移除 deque 尾部比 nums[i] 小的元素下标
            while (!deque.isEmpty() && nums[deque.peekLast()] < nums[i]) {
                deque.pollLast();
            }
            
            // 3. 当前元素下标入队
            deque.offerLast(i);
            
            // 4. 当 i >= k - 1 时，窗口已经形成，记录最大值
            if (i >= k - 1) {
                res[ri++] = nums[deque.peekFirst()];
            }
        }
        
        return res;
    }
}
```

------

##### 📌 示例

```java
public class Main {
    public static void main(String[] args) {
        Solution s = new Solution();
        int[] nums = {1, 3, -1, -3, 5, 3, 6, 7};
        int k = 3;
        int[] res = s.maxSlidingWindow(nums, k);
        System.out.println(Arrays.toString(res));
        // 输出 [3, 3, 5, 5, 6, 7]
    }
}
```

------

##### 📊 时空复杂度

- **时间复杂度：O(n)**
  - 每个元素最多被加入和移除队列一次。
- **空间复杂度：O(k)**
  - 队列最多保存 k 个元素的下标。

------









### 76.最小覆盖子串

----

#### 思路

这个题目本质是**滑动窗口 + 哈希表**问题，目标是在 `s` 中找到一个最短子串，包含 `t` 中所有字符（包括重复字符）。

------

##### 🔑 Java 实现（滑动窗口法）

```java
import java.util.HashMap;

public class Solution {
    public String minWindow(String s, String t) {
        if (s == null || t == null || s.length() < t.length()) return "";

        // 1. 用 HashMap 统计 t 中字符的频次
        HashMap<Character, Integer> need = new HashMap<>();
        for (char c : t.toCharArray()) {
            need.put(c, need.getOrDefault(c, 0) + 1);
        }

        HashMap<Character, Integer> window = new HashMap<>(); // 窗口中字符频次
        int left = 0, right = 0; // 窗口左右指针
        int valid = 0; // 窗口中满足条件的字符种类数
        int start = 0, len = Integer.MAX_VALUE; // 记录最小子串的位置和长度

        while (right < s.length()) {
            char c = s.charAt(right);
            right++; // 右移窗口

            // 如果 c 是 t 中需要的字符，更新窗口频次
            if (need.containsKey(c)) {
                window.put(c, window.getOrDefault(c, 0) + 1);
                // 如果窗口内该字符数量刚好满足需求，valid +1
                if (window.get(c).intValue() == need.get(c).intValue()) {
                    valid++;
                }
            }

            // 当窗口包含 t 所有字符时，尝试缩小窗口
            while (valid == need.size()) {
                // 更新最小子串
                if (right - left < len) {
                    start = left;
                    len = right - left;
                }

                char d = s.charAt(left);
                left++; // 左移窗口

                // 如果 d 是 t 中需要的字符，更新窗口频次
                if (need.containsKey(d)) {
                    if (window.get(d).intValue() == need.get(d).intValue()) {
                        valid--; // 窗口内不再完全满足需求
                    }
                    window.put(d, window.get(d) - 1);
                }
            }
        }

        // 返回结果
        return len == Integer.MAX_VALUE ? "" : s.substring(start, start + len);
    }
}
```

------

##### 📊 时空复杂度

- **时间复杂度**：O(n + m)
  - n = s.length(), m = t.length()
  - 窗口每个字符最多访问两次（右指针进，左指针出）。
- **空间复杂度**：O(n + m)
  - 需要两个哈希表存储 t 的字符频次和窗口内字符频次。

------









## 普通数组

### 53.最大子数组和

---

#### 思路

用 Java 写法，可以用 **普通数组** 来实现，最常用的是 **动态规划（Kadane 算法）**。

##### 核心思路：

1. `currentSum` 记录以当前元素结尾的最大子数组和。

2. 每次遇到新元素，判断是**继续累加**还是**从当前元素重新开始**：

   ```java
   currentSum = Math.max(nums[i], currentSum + nums[i]);
   ```

3. `maxSum` 用来记录全局最大值。

下面是完整示例：

```java
public class Solution {
    public int maxSubArray(int[] nums) {
        if (nums == null || nums.length == 0) {
            return 0; // 或者根据题意抛异常
        }

        int maxSum = nums[0]; // 全局最大和
        int currentSum = nums[0]; // 当前子数组和

        for (int i = 1; i < nums.length; i++) {
            // 如果当前子数组和为负数，就舍弃，重新开始
            currentSum = Math.max(nums[i], currentSum + nums[i]);
            // 更新全局最大和
            maxSum = Math.max(maxSum, currentSum);
        }

        return maxSum;
    }

}
```

##### 时空复杂度

时间复杂度 O(n)，空间复杂度 O(1)

---







### 56.合并区间

---

#### 思路

##### 核心思路

1. **排序**：按区间起点排序，保证后面的区间不会完全落在前面的左侧。
2. **合并**：遍历每个区间：
   - 如果当前区间与前一个合并区间不重叠，直接加入结果。
   - 如果有重叠，更新上一个合并区间的右端点为最大值。
3. **返回结果**：把 `List<int[]>` 转成二维数组。

------

##### Java 示例

```java
import java.util.Arrays;
import java.util.ArrayList;
import java.util.List;

public class Solution {
    public int[][] merge(int[][] intervals) {
        if (intervals == null || intervals.length == 0) {
            return new int[0][];
        }

        // 1. 按照区间起点排序
        Arrays.sort(intervals, (a, b) -> a[0] - b[0]);

        List<int[]> merged = new ArrayList<>();

        // 2. 遍历区间，合并重叠区间
        for (int[] interval : intervals) {
            // 如果 merged 为空，或者当前区间不重叠，直接加入
            if (merged.isEmpty() || merged.get(merged.size() - 1)[1] < interval[0]) {
                merged.add(interval);
            } else {
                // 存在重叠，更新末端为最大值
                merged.get(merged.size() - 1)[1] = Math.max(merged.get(merged.size() - 1)[1], interval[1]);
            }
        }

        // 3. 转换成二维数组返回
        return merged.toArray(new int[merged.size()][]);
    }

}
```

------

💡 **关键点**：

- 排序是必须的，否则无法保证线性合并。
- 合并判断用：

```java
merged.get(merged.size() - 1)[1] < interval[0]
```

来判断是否重叠。

------

##### 时空复杂度

**总时间复杂度**：

O(nlog⁡n+n)=O(nlogn)

**总空间复杂度**：

O(n)

---









### 189.轮转数组

------

#### 💡 思路 1：使用反转法（最优解）

时间复杂度 **O(n)**，空间复杂度 **O(1)**，只用普通数组。

##### 🔹思路

1. 先整体反转数组
2. 再反转前 k 个
3. 最后反转剩下的 n - k 个

👉 这样实现右移效果。

------

##### ✅ Java 实现

```java
import java.util.Arrays;

public class Solution {
    public void rotate(int[] nums, int k) {
        int n = nums.length;
        k = k % n; // 处理 k 大于数组长度的情况

        // 1. 反转整个数组
        reverse(nums, 0, n - 1);

        // 2. 反转前 k 个元素
        reverse(nums, 0, k - 1);

        // 3. 反转剩下的 n - k 个元素
        reverse(nums, k, n - 1);
    }

    // 辅助函数：反转数组中从 start 到 end 的部分
    private void reverse(int[] nums, int start, int end) {
        while (start < end) {
            int temp = nums[start];
            nums[start] = nums[end];
            nums[end] = temp;
            start++;
            end--;
        }
    }

}
```

------

##### 🧠 运行过程（以 [1,2,3,4,5,6,7], k=3 为例）

| 步骤  | 操作      | 结果            |
| ----- | --------- | --------------- |
| 初始  | 原数组    | [1,2,3,4,5,6,7] |
| 第1步 | 整体反转  | [7,6,5,4,3,2,1] |
| 第2步 | 反转前3个 | [5,6,7,4,3,2,1] |
| 第3步 | 反转后4个 | [5,6,7,1,2,3,4] |

------

##### ⚙️ 复杂度分析

| 类型       | 复杂度                          |
| ---------- | ------------------------------- |
| 时间复杂度 | O(n) （每个元素最多被交换两次） |
| 空间复杂度 | O(1) （原地操作）               |

------







### 238. 除自身以外数组的乘积

------

#### ✅ 思路（前缀积 + 后缀积）

我们可以用 **两次遍历** 来完成：
 1️⃣ 第一次从左往右，计算每个位置左边所有数的乘积。
 2️⃣ 第二次从右往左，用一个变量保存右边所有数的乘积，再与左边的乘积相乘。

------

##### ✅ Java 实现（普通数组，O(1) 额外空间）

```java
import java.util.Arrays;

public class Solution {
    public int[] productExceptSelf(int[] nums) {
        int n = nums.length;
        int[] answer = new int[n];

        // 1️⃣ 先计算左边所有数的乘积
        // answer[i] 表示 nums[0...i-1] 的乘积
        answer[0] = 1;
        for (int i = 1; i < n; i++) {
            answer[i] = answer[i - 1] * nums[i - 1];
        }

        // 2️⃣ 从右往左遍历，用 rightProduct 保存右边所有数的乘积
        int rightProduct = 1;
        for (int i = n - 1; i >= 0; i--) {
            answer[i] = answer[i] * rightProduct;
            rightProduct *= nums[i];
        }

        return answer;
    }

}
```

------

##### 🧠 运行过程（示例：nums = [1,2,3,4]）

|   i    | 左乘积 (`answer`) |
| :----: | :---------------: |
|  初始  |   [1, 1, 1, 1]    |
| 左遍历 |   [1, 1, 2, 6]    |
| 右遍历 |   [24, 12, 4 1]   |

------

##### ⚙️ 复杂度分析

| 类型       | 复杂度                              |
| ---------- | ----------------------------------- |
| 时间复杂度 | **O(n)** （两次线性遍历）           |
| 空间复杂度 | **O(1)** （结果数组不计入额外空间） |

------

💡 **关键点：**

- 用 `answer` 存左侧乘积；
- 用一个变量 `rightProduct` 动态乘右侧值；
- 不用额外数组，也不用除法。

------











### 41.缺失的第一个正数

------

#### 💡 思路：索引映射（原地哈希）

这是个“数组索引 + 值对齐”的技巧题。
 关键观察点：

> 若数组长度为 n，则结果一定在 `[1, n+1]` 之间。

也就是说：

- 如果数组中包含 1 到 n 的所有数 → 答案是 n+1
- 否则，缺的那个数就是答案

------

##### ✅ 核心算法（索引交换法）

目标：
 让数字 `x` 放到索引 `x-1` 的位置上
 （例如数字 `1` 放 index 0，数字 `2` 放 index 1，……）

------

##### 🔹算法步骤

1️⃣ **清洗数据**：
 把小于等于 0 或大于 n 的数替换成一个无效值（比如 n+1）

2️⃣ **原地放置**：
 用 `while` 循环把数字放到正确的位置上：
 `nums[nums[i] - 1] = nums[i]`

3️⃣ **扫描寻找缺口**：
 找到第一个 `nums[i] != i + 1` 的位置，即答案是 `i + 1`

4️⃣ 如果都匹配，说明缺的是 `n + 1`

------

##### ✅ Java 实现（普通数组，O(1)空间）

```java
import java.util.Arrays;

public class Solution {
    public int firstMissingPositive(int[] nums) {
        int n = nums.length;

        // Step 1: 把无效的数字（<=0 或 >n）都设成 n+1
        for (int i = 0; i < n; i++) {
            if (nums[i] <= 0 || nums[i] > n) {
                nums[i] = n + 1;
            }
        }

        // Step 2: 原地标记
        for (int i = 0; i < n; i++) {
            int val = Math.abs(nums[i]);
            if (val <= n) {
                // 标记索引 val-1 位置为负数，表示该数存在
                if (nums[val - 1] > 0) {
                    nums[val - 1] = -nums[val - 1];
                }
            }
        }

        // Step 3: 找到第一个正数的位置
        for (int i = 0; i < n; i++) {
            if (nums[i] > 0) {
                return i + 1;
            }
        }

        // Step 4: 如果都被标记，返回 n+1
        return n + 1;
    }

}
```

------

##### 🔍 运行逻辑讲解（例子：nums = [3,4,-1,1]）

| 步骤               | 操作                        | 数组状态     |
| ------------------ | --------------------------- | ------------ |
| 初始               | [3,4,-1,1]                  | —            |
| Step1 清洗         | [-1→5] → [3,4,5,1]          | (因为 n=4)   |
| Step2 标记         | 标记 3、4、1                | [-1,4,-5,-1] |
| Step3 找第一个正数 | index=1 是正数 → 缺失的是 2 |              |

✅ 输出：2

------

##### ⚙️ 复杂度分析

| 类型       | 复杂度                          |
| ---------- | ------------------------------- |
| 时间复杂度 | **O(n)** （每个元素访问常数次） |
| 空间复杂度 | **O(1)** （原地修改数组）       |

------

##### 🧠 总结逻辑

1. 筛掉无关数据（负数、太大数）
2. 用负号表示“这个数存在过”
3. 第一个没被标记的位置，就是缺的最小正数

------









## 矩阵

### 73.矩阵置零

---

#### 详细思路

##### 💡 思路分析

最直观的做法：

1. 扫一遍矩阵，把所有为 0 的位置记录下来。
2. 再扫一遍，根据这些记录把对应的行列置 0。

👉 问题是，这样需要额外 O(m+n) 的空间来存记录（比如用两个数组 `row[]`、`col[]` 标记哪些行、列要置零），
 但题目希望我们用 **O(1) 空间**，也就是 **原地操作**。

------

##### 🚀 优化思路 —— 用矩阵的第一行和第一列来做标记

既然不能额外开数组，我们可以**借用矩阵本身的第一行和第一列**来记录哪些行和列需要置零。

------

##### 🌟 核心思想

假设 `matrix[i][j] == 0`，
 那么：

- 我们把这一行的标记 `matrix[i][0]` 设为 0
- 把这一列的标记 `matrix[0][j]` 设为 0

这就相当于：

> “第 i 行、第 j 列都要被置零”的信息，已经被写在矩阵的第一列和第一行里了。

------

##### 🧠 步骤拆解

###### ✅ 第一步：记录第一行和第一列是否需要被清零

因为第一行、第一列我们要当作“标记位”使用，
 所以要提前知道它们自己是否本来就需要清零。

我们用两个布尔变量记录：

```java
boolean firstRowZero = false;
boolean firstColZero = false;
```

然后分别检查：

- 如果第一行中有 0，则 `firstRowZero = true`
- 如果第一列中有 0，则 `firstColZero = true`

------

###### ✅ 第二步：标记要清零的行列（从第 1 行、第 1 列开始）

遍历矩阵中除开第一行和第一列的元素：

```java
for (int i = 1; i < m; i++) {
    for (int j = 1; j < n; j++) {
        if (matrix[i][j] == 0) {
            matrix[i][0] = 0; // 标记第 i 行
            matrix[0][j] = 0; // 标记第 j 列
        }
    }
}
```

------

###### ✅ 第三步：根据标记清零（同样从第 1 行、第 1 列开始）

现在，我们再遍历矩阵：

- 如果某个位置的 **行标记** 或 **列标记** 为 0，就把这个位置清零。

```java
for (int i = 1; i < m; i++) {
    for (int j = 1; j < n; j++) {
        if (matrix[i][0] == 0 || matrix[0][j] == 0) {
            matrix[i][j] = 0;
        }
    }
}
```

------

###### ✅ 第四步：处理第一行和第一列

最后，我们根据一开始记录的两个布尔值，
 决定是否把第一行、第一列整体清零：

```java
if (firstRowZero) {
    for (int j = 0; j < n; j++) matrix[0][j] = 0;
}

if (firstColZero) {
    for (int i = 0; i < m; i++) matrix[i][0] = 0;
}
```

------

##### 🧾 举例说明

输入：

```
[
 [1,1,1],
 [1,0,1],
 [1,1,1]
]
```

执行流程：

1️⃣ 第一行、第一列没有 0 → firstRowZero = false, firstColZero = false
 2️⃣ 遍历剩余元素：

- 发现 `matrix[1][1] == 0`
- 标记：`matrix[1][0] = 0`, `matrix[0][1] = 0`

矩阵变成：

```
[
 [1,0,1],
 [0,0,1],
 [1,1,1]
]
```

3️⃣ 根据标记清零：

- 第二行因为 `matrix[1][0]==0`，整行置 0
- 第二列因为 `matrix[0][1]==0`，整列置 0

得到：

```
[
 [1,0,1],
 [0,0,0],
 [1,0,1]
]
```

4️⃣ 因为 `firstRowZero=false`、`firstColZero=false`，第一行第一列不变。✅

------

##### 🧮 复杂度分析

| 类型 | 复杂度                    |
| ---- | ------------------------- |
| 时间 | O(m × n) （两次遍历矩阵） |
| 空间 | O(1) （原地标记）         |

------

##### ✅ 总结一句话

> 用第一行、第一列当标记，省掉额外数组空间；
>  先标记，再清零，最后单独处理第一行和第一列。

------





#### 思路总结与代码

下面给出常用且 **原地 O(1) 额外空间** 的解法（利用第 0 行和第 0 列做标记）。思路简要：

1. 先扫描第一行和第一列，记录它们是否本身需要被置为 0（用两个布尔变量 `firstRowZero`、`firstColZero`）。
2. 遍历其余元素（从 `i=1, j=1` 开始），若遇到 0，则把对应的第 `i` 行第 0 列和第 `j` 列第 0 行标记为 0。
3. 再次遍历（从 `i=1, j=1`），如果对应的行或列标记为 0，则把元素置为 0。
4. 最后根据 `firstRowZero`、`firstColZero` 决定是否把第一行/第一列全部置为 0。

##### 代码

```java
class Solution {
    public void setZeroes(int[][] matrix) {
        if (matrix == null || matrix.length == 0) return;
        int m = matrix.length; // 行数
        int n = matrix[0].length; // 列数

        boolean firstRowZero = false; // 第一行标记
        boolean firstColZero = false; // 第一列标记

        // 检查第一列是否有 0
        for (int i = 0; i < m; i++) {
            if (matrix[i][0] == 0) {
                firstColZero = true;
                break;
            }
        }

        // 检查第一行是否有 0
        for (int j = 0; j < n; j++) {
            if (matrix[0][j] == 0) {
                firstRowZero = true;
                break;
            }
        }

        // 用第一行和第一列作为标记（从 1 开始）
        for (int i = 1; i < m; i++) {
            for (int j = 1; j < n; j++) {
                if (matrix[i][j] == 0) {
                    matrix[i][0] = 0; // 标记第 i 行
                    matrix[0][j] = 0; // 标记第 j 列
                }
            }
        }

        // 根据标记置零（从 1 开始，避免覆盖标记）
        for (int i = 1; i < m; i++) {
            for (int j = 1; j < n; j++) {
                if (matrix[i][0] == 0 || matrix[0][j] == 0) { // 看标记位
                    matrix[i][j] = 0;
                }
            }
        }

        // 处理第一列
        if (firstColZero) {
            for (int i = 0; i < m; i++) {
                matrix[i][0] = 0;
            }
        }

        // 处理第一行
        if (firstRowZero) {
            for (int j = 0; j < n; j++) {
                matrix[0][j] = 0;
            }
        }
    }
}
```

##### 复杂度

- 时间复杂度：O(m * n)
- 空间复杂度：O(1)（不计输入矩阵本身）

示例（便于理解）：
 输入：

```
[
 [1,1,1],
 [1,0,1],
 [1,1,1]
]
```

输出：

```
[
 [1,0,1],
 [0,0,0],
 [1,0,1]
]
```





### 54.螺旋矩阵

------

#### 💡 思路讲解

可以把螺旋遍历看作一层一层地“剥洋葱”。

我们用四个变量来记录当前“边界”：

- `top`    —— 当前未遍历的最上边界
- `bottom` —— 当前未遍历的最下边界
- `left`   —— 当前未遍历的最左边界
- `right`  —— 当前未遍历的最右边界

然后循环遍历：

1. 从左到右 → 遍历上边界
2. 从上到下 → 遍历右边界
3. 从右到左 → 遍历下边界（如果还有行）
4. 从下到上 → 遍历左边界（如果还有列）

每走完一圈，就缩小边界范围。

------

##### ✅ Java 代码实现

```java
import java.util.*;

class Solution {
    public List<Integer> spiralOrder(int[][] matrix) {
        List<Integer> res = new ArrayList<>();
        if (matrix == null || matrix.length == 0) return res;

        int top = 0;
        int bottom = matrix.length - 1;
        int left = 0;
        int right = matrix[0].length - 1;

        while (top <= bottom && left <= right) {
            // 1. 从左到右
            for (int j = left; j <= right; j++) {
                res.add(matrix[top][j]);
            }
            top++;

            // 2. 从上到下
            for (int i = top; i <= bottom; i++) {
                res.add(matrix[i][right]);
            }
            right--;

            // 3. 从右到左（要确保还有行）
            if (top <= bottom) {
                for (int j = right; j >= left; j--) {
                    res.add(matrix[bottom][j]);
                }
                bottom--;
            }

            // 4. 从下到上（要确保还有列）
            if (left <= right) {
                for (int i = bottom; i >= top; i--) {
                    res.add(matrix[i][left]);
                }
                left++;
            }
        }

        return res;
    }
}
```

------

##### 🧮 复杂度分析

| 类型 | 复杂度                          |
| ---- | ------------------------------- |
| 时间 | O(m × n) （每个元素访问一次）   |
| 空间 | O(1) （除返回结果外无额外空间） |

------

##### 🧠 一句话总结

> 用四个边界控制遍历方向，按「右 → 下 → 左 → 上」顺序螺旋访问，每走完一圈就收缩边界。

------





#### 问题：为什么只有后两个要看是否还有行、列？

非常好的问题 👏
 这个细节是 **螺旋矩阵算法的关键点之一**，很多人第一次写都会漏掉。
 我来帮你清楚地解释为什么只有 **“从右到左”** 和 **“从下到上”** 两步要加判断。

------

#### 🌪️ 回顾一下遍历顺序

在每一圈里，我们是这样访问矩阵的四个方向：

1️⃣ 从左 → 右（上边）
 2️⃣ 从上 → 下（右边）
 3️⃣ 从右 → 左（下边）
 4️⃣ 从下 → 上（左边）

然后缩小边界，进入下一圈。

------

#### 🧩 关键问题：什么时候会「重复访问」或「越界」？

假设矩阵的行列不相等，或者最后只剩一行 / 一列，
 那么前两步执行完后，可能已经遍历完所有元素了。
 如果这时还不加判断直接执行第 3、4 步，就会重复访问或越界。

------

##### ✅ 举例 1：只剩一行的情况

矩阵：

```
[1, 2, 3]
```

第一次循环：

- top = 0, bottom = 0, left = 0, right = 2

执行：
 1️⃣ 从左到右 → 输出 [1, 2, 3]
 → top++ ⇒ top = 1

此时 **top > bottom**，说明已经没有更多的行了。

如果接着执行第 3 步（从右到左），就会访问不存在的行 —— 越界 ❌

------

##### ✅ 举例 2：只剩一列的情况

矩阵：

```
1  
2  
3
```

第一次循环：

- top = 0, bottom = 2, left = 0, right = 0

执行：
 1️⃣ 从左到右 → 输出 [1]
 → top++ ⇒ top = 1

2️⃣ 从上到下 → 输出 [2, 3]
 → right-- ⇒ right = -1

此时 **left > right**，说明没有更多的列了。

如果继续执行第 4 步（从下到上），又会访问不存在的列 —— 越界 ❌

------

##### 🧠 因此：

- 第 1 步和第 2 步（从上边、右边）一定合法，因为它们是每圈的开始；
- 第 3 步（从右到左）要先检查是否还存在未访问的行（`top <= bottom`）；
- 第 4 步（从下到上）要先检查是否还存在未访问的列（`left <= right`）。

------

##### ✅ 总结一句话

> “从右到左” 和 “从下到上” 是可选的收尾步骤，
>  因为在螺旋遍历的最后一圈里，可能只剩一行或一列，
>  如果不判断就会重复访问或越界。

------









### 48.旋转图像

#### 💡 思路讲解（最经典方法）

要想顺时针旋转 90°，有一个非常漂亮的两步法：

1️⃣ **转置矩阵（transpose）**
 — 把行列互换，即 `matrix[i][j]` ↔ `matrix[j][i]`
 （主对角线对称）

2️⃣ **反转每一行（reverse each row）**
 — 把每一行的元素顺序反过来。

这样就能得到顺时针旋转 90° 的结果。

------

##### 🧩 举例演示

原始矩阵：

```
1 2 3
4 5 6
7 8 9
```

**第一步：转置**

```
1 4 7
2 5 8
3 6 9
```

**第二步：每行反转**

```
7 4 1
8 5 2
9 6 3
```

✅ 完成顺时针旋转。

------

##### ✅ Java 实现

```java
class Solution {
    public void rotate(int[][] matrix) {
        int n = matrix.length;

        // 1. 转置矩阵
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {
                int temp = matrix[i][j];
                matrix[i][j] = matrix[j][i];
                matrix[j][i] = temp;
            }
        }

        // 2. 反转每一行
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n / 2; j++) {
                int temp = matrix[i][j];
                matrix[i][j] = matrix[i][n - 1 - j];
                matrix[i][n - 1 - j] = temp;
            }
        }
    }
}
```

------

##### 🧮 复杂度分析

| 类型       | 复杂度                          |
| ---------- | ------------------------------- |
| 时间复杂度 | O(n²)（访问矩阵中每个元素一次） |
| 空间复杂度 | O(1)（原地操作，无额外空间）    |

------

##### ✅ 一句话总结

> **先转置，再反转每一行**，就能原地实现矩阵顺时针旋转 90°。

------

如果你想实现**逆时针旋转 90°**，也很简单：

> **先转置，再反转每一列**。